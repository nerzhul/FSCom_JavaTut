package windows.actions;

import java.awt.event.MouseEvent;
import java.awt.event.MouseListener;

import javax.swing.JList;
import javax.swing.JMenuItem;
import javax.swing.JPopupMenu;
import javax.swing.JTree;

import windows.window.form_conversation_avec;
public class Clicks_Contact implements MouseListener {

	private JTree arbre;
	public Clicks_Contact(JTree tree) {
		this.arbre = tree;
	}

	public void mouseClicked(MouseEvent e) {
		arbre.getSelectionPath();
		//action sur double click d'un contact
		if(e.getClickCount() == 2 && curr.isLeaf()){
			Object contact = arbre.getPathForLocation(e.getX(), e.getY());;
			
			new form_conversation_avec(contact);
			System.out.println("double cliqué sur " + contact);
		}
		   
	}


	public void mouseReleased(MouseEvent e) {
		if (e.getButton() == 3){
			JPopupMenu menu = new JPopupMenu();
			
			//int index = arbre.getPathForLocation(e.getX(), e.getY());
	        //arbre.setSelectedIndex(index);
			Object contact = arbre.getPathForLocation(e.getX(), e.getY());;
			
			JMenuItem envoi = new JMenuItem("Envoyer un message");
			envoi.addActionListener(new Clicks_Contact_Envoi(contact));
			JMenuItem supprimer = new JMenuItem("Supprimer");
			supprimer.addActionListener(new Clicks_Contact_Supprimer(contact));
			JMenuItem bloquer = new JMenuItem("Bloquer");
			bloquer.addActionListener(new Clicks_Contact_Bloquer(contact));
			JMenuItem details = new JMenuItem("Voir les détails");
			details.addActionListener(new Clicks_Contact_Details(contact));
			menu.add(envoi);
			menu.add(supprimer);
			menu.add(bloquer);
			menu.add(details);
			
	        menu.show (e.getComponent(),e.getX(),e.getY());

	        System.out.println("clique droit sur " + contact);
		}
	}

	@Override
	public void mouseEntered(MouseEvent e) {
		// TODO Auto-generated method stub
		
	}

	@Override
	public void mouseExited(MouseEvent e) {
		// TODO Auto-generated method stub
		
	}

	@Override
	public void mousePressed(MouseEvent e) {
		// TODO Auto-generated method stub
		
	}

}
