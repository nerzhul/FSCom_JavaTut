package msn.pageacceuil;
import msn.menu.*;
import msn.pageheritage.Page;

import java.awt.Color;
import java.awt.FlowLayout;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.*;


public class Pageacceuil extends Page implements ActionListener
{
	private static final long serialVersionUID = 1L;
	private JTextField mail;
	private JPasswordField password;
	private JLabel label;
	private JLabel label2;
	
	
	
		public Pageacceuil()
		{	super();
			fenetreprincipale();//On initialise notre fenetre
		}
	
		private JPanel contenufenetreconnec()
		{
			JPanel panel = new JPanel();
			
			panel.setLayout(new FlowLayout());
			panel.setBackground(Color.blue); //on change la couleur de fond de la fenetre
			JLabel labelbienvenue = new JLabel("Bienvenue dans notre logiciel de messagerie"); //message d'acceuil
			panel.add(labelbienvenue);
					
			//on cree le premier champ demandant l'adresse e-mail
				mail = new JTextField();
				panel.add(mail);
				mail.setColumns(20);
				label = new JLabel("Adresse e-mail");
				panel.add(label);
				
				//on cree le second champ demandant le mot de passe avec un champ password
				password = new JPasswordField();
				password.setColumns(20);
				panel.add(password);
							
				label2 = new JLabel("Mot de passe");
				panel.add(label2);

					
				JButton bouton = new JButton(new Connexion(this, "connexion")); //on ajoute le bouton de connexion
				panel.add(bouton); 
			
			
			return panel;
		}
		
		
		 //fonction pour r�cup�rer les champs adresse e-mail et mot de passe
				public JTextField getTextField()
				{
					return mail;
				}
				
				public JTextField getpassword()
				{
					return password;
				}
				
				private void fenetreprincipale()
				{
					setTitle("Page de connexion"); //On donne un titre � l'application
					setSize(400,200); //On donne une taille � notre fen�tre
					setLocationRelativeTo(null); //On centre la fen�tre sur l'�cran
					setResizable(false); //On interdit la redimensionnement de la fen�tre
					setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //On dit � l'application de se fermer lors du clic sur la croix
					setContentPane(Pageglobale());
					setContentPane(contenufenetreconnec()); //on appelle la m�thode qui ajoutera du texte dans notre fenetre
				}

				@Override
				public void actionPerformed(ActionEvent e) {
					// TODO Auto-generated method stub
					
				}
	}
				
